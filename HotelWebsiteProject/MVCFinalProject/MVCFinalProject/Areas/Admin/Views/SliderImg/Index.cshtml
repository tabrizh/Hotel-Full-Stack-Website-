@*@model List<SliderImg>

<div class="content-wrapper">
    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Categories List</h4>
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>Image Url</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var sliderImg in Model)
                                {
                                    <tr>
                                        <td><img src="~/img/@sliderImg.ImgUrl" style="width: 200px; height: 200px; object-fit: contain; border-radius: 50%"/></td>
                                        <td><a class="btn btn-primary" asp-controller="SliderImg" asp-action="Details" asp-route-id="@sliderImg.Id">Details</a></td>
                                        <td><a class="btn btn-success" asp-controller="SliderImg" asp-action="Update" asp-route-id="@sliderImg.Id">Update</a></td>
                                        <td><a class="btn btn-danger" asp-action="Delete" asp-route-id="@sliderImg.Id">Delete</a></td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>*@



