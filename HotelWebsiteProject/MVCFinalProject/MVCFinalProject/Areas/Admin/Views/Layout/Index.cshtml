@*@model List<Layout>

<div class="content-wrapper">
    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Layout Links List</h4>
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>Logo</th>
                                    <th>Linkedin Link</th>
                                    <th>Facebook Link</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var link in Model)
                                {
                                    <tr>
                                        <td style="font-size: 10px"> <img src="~/img/@link.Logo" style="width: 100px; height: 100px; object-fit:contain"/></td>
                                        <td style="font-size: 10px">@link.LinkedInURL</td>
                                        <td style="font-size: 10px">@link.FacebookURL</td>
                                        <td><a class="btn btn-primary" asp-controller="Layout" asp-action="Details" asp-route-id="@link.Id">Details</a></td>
                                        <td><a class="btn btn-success">Update</a></td>
                                        <td><a class="btn btn-danger" asp-controller="Layout" asp-action="Delete" asp-route-id="@link.Id">Delete</a></td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>*@



